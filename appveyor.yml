version: 1.0.{build}

platform:
  - x64

cache:
  - node_modules
  - '%APPDATA%\npm-cache'
  - '%USERPROFILE%\.electron'

install:
  - ps: Install-Product node 14 x64
  - npm install

build_script:
  - npm run release

test: off

branches:
  only:
    - master
